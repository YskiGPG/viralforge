# ViralForge dependencies — Python 3.11+
# Versions pinned to a known-good lower bound; >= lets you pick up fixes.

anthropic>=0.109.1              # Anthropic Python SDK (Skills + Tools)
google-api-python-client>=2.197.0  # YouTube Data API v3
praw>=7.8.2                     # Reddit API wrapper
streamlit>=1.58.0              # Chat frontend with streaming
python-dotenv>=1.2.2          # Loads keys from .env
